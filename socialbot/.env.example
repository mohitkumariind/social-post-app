# Copy to `.env.local` in this folder (gitignored).
NEXT_PUBLIC_SUPABASE_URL=https://tkxtodjbfsqvfipsrppk.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Admin: storage, profiles checks, POST /api/notifications/send — server-only, never expose to the browser bundle.
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Optional: higher Expo Push rate limits (https://expo.dev/accounts/[account]/settings/access-tokens)
# EXPO_ACCESS_TOKEN=
