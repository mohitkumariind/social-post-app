/**
 * Android notification channel id for Expo/FCM-delivered pushes.
 * Must match `Notifications.setNotificationChannelAsync` in mobile `app/_layout.tsx`.
 */
export const ANDROID_NOTIFICATION_CHANNEL_ID = 'default' as const;
